import { FC } from 'react';
import icon from '../../shared/assets/img/postgresql-icon.png';

const PostgreSQLIcon: FC = () => {
  return <img src={icon} alt="Mantine UI icon" />;
};

export default PostgreSQLIcon;
