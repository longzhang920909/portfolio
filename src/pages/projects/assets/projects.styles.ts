import styled from 'styled-components';
import Box from '../../../ui/box';

export const StyledProjectList = styled(Box)(() => ({
  marginTop: '30px',
  display: 'flex',
  flexDirection: 'column',
  gap: '30px',
}));
